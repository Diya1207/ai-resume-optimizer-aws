# Terraform module files for AI Resume Optimizer

This folder contains Terraform configuration for deploying AWS resources:
- main.tf : provider and example S3 resource
- variables.tf : default variables
- outputs.tf : outputs for resources
- subfolders for modular resources (lambda, dynamodb, step-functions, iam, s3, ses, eventbridge)
